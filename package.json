{
  "name": "dflow-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "generate:types": "pnpm --filter @dflow/web run generate:types",
    "generate:importmap": "pnpm --filter @dflow/web run generate:importmap",
    "payload": "pnpm --filter @dflow/web run payload"
  },
  "devDependencies": {
    "@content-collections/next": "^0.2.7",
    "@turbo/gen": "^2.5.8",
    "eslint": "^9.0.0",
    "prettier": "^3.4.2",
    "turbo": "^2.6.1",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "payload": "3.54.0"
  }
}
